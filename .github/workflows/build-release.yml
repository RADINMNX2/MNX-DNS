name: Build & Release MNX DNS

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install --force

      - name: Ensure build directories exist
        shell: cmd
        run: |
          if not exist "binaries\windivert" mkdir "binaries\windivert"
          if not exist "binaries\wireguard" mkdir "binaries\wireguard"
          if not exist "binaries\aether" mkdir "binaries\aether"
          echo.>"binaries\windivert\.gitkeep"
          echo.>"binaries\wireguard\.gitkeep"
          echo.>"binaries\aether\.gitkeep"

      - name: Typecheck
        shell: cmd
        run: node_modules\.bin\tsc --noEmit

      - name: Vite Build
        shell: cmd
        run: node_modules\.bin\vite build

      - name: Electron Builder
        shell: cmd
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node_modules\.bin\electron-builder --win --x64 -p never

      - name: List artifacts
        shell: cmd
        run: dir release /s /b

      - name: Upload Installer
        uses: actions/upload-artifact@v4
        with:
          name: MNX-DNS-Windows-x64
          path: release/*.exe
          if-no-files-found: warn
          retention-days: 30

  publish:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v') || (github.ref == 'refs/heads/main' && github.event_name == 'push')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: MNX-DNS-Windows-x64
          path: artifacts

      - name: Publish Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ startsWith(github.ref, 'refs/tags/v') && github.ref_name || 'latest-build' }}
          name: ${{ startsWith(github.ref, 'refs/tags/v') && format('MNX DNS {0}', github.ref_name) || 'Latest Build' }}
          files: artifacts/*
          draft: false
          prerelease: true
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
